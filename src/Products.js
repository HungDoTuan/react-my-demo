import { ChakraProvider } from "@chakra-ui/provider"

const Products = () => {
    return (
        <ChakraProvider>
            <h2>hello from Products</h2>
        </ChakraProvider>
    )
}
export default Products